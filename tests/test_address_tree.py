import pytest


"""
from thinc.ml.feature_map import AddressTree

@pytest.fixture
def tree():
    return AddressTree()


def test_insert(tree):
    feat = (5,)
    clas = 20
    assert tree.lookup_tuple(feat) == 0
    tree.insert_tuple(feat, clas)
    assert tree.first == 5
    assert tree.last == 6
    assert tree.lookup_tuple(feat) == clas
    feat = (10, 45, 2)
    clas = 10
    assert tree.lookup_tuple(feat) == 0
    tree.insert_tuple(feat, clas)
    assert tree.lookup_tuple(feat) == clas
"""

